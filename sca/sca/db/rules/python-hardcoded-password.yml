id: python-hardcoded-password
message: "Hardcoded password/credentials detected"
severity: critical
language: Python
rule:
  pattern-either:
    - pattern: $VAR = "...$PASSWORD..."
    - pattern: $VAR = "...$PASSWORD..."
    - pattern: $VAR = "...$KEY..."
    - pattern: $VAR = "...$SECRET..."
